{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "watch": "npm run build -- -w",
    "live": "cross-env balena push -g $(grep REACT_APP_LIVE_IP ../.env | cut -d '=' -f2)"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mplayer": "^2.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.8",
    "cross-env": "^7.0.2"
  }
}
